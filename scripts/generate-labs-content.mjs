import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { createRequire } from 'node:module';
import { MODELS } from '../js/labs/config.mjs';
import { DECISION_EXAMPLES } from '../js/labs/decisions.mjs';
import { LABS_DESCRIPTION, LABS_FAQ } from '../js/labs/content.mjs';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const CHECK = process.argv.includes('--check');
const require = createRequire(import.meta.url);
const { siteNavigation, siteNavAssets } = require('./site-navigation.js');
const BASE = 'https://localclaw.io';
const GUIDE = '/guides/run-llm-in-browser';
const IMAGE = `${BASE}/images/labs-browser-playground.jpg`;
const escape = text => String(text).replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char]));
const json = value => JSON.stringify(value).replace(/</g, '\\u003c');
const size = bytes => bytes >= 1e9 ? `${(bytes / 1e9).toFixed(2)} GB` : `${Math.round(bytes / 1e6)} MB`;
const rows = MODELS.map(model => `<tr><th scope="row"><a href="https://huggingface.co/${model.repo}/tree/${model.revision}" target="_blank" rel="noopener noreferrer">${escape(model.name)}</a></th><td>${escape(model.quantization)}</td><td>${size(model.bytes)}</td><td>${escape(model.memory)}</td></tr>`).join('\n');
const table = `<div class="labs-table-wrap" tabindex="0" role="region" aria-label="Browser model comparison, horizontally scrollable"><table class="labs-model-table"><caption>Five downloadable browser presets · Apache 2.0 weights · fixed revisions</caption><thead><tr><th scope="col">Model / source</th><th scope="col">Quantization</th><th scope="col">Download</th><th scope="col">Memory guidance</th></tr></thead><tbody>${rows}</tbody></table></div>`;
const breadcrumbs = (name, url) => ({ '@type': 'BreadcrumbList', itemListElement: [{ '@type': 'ListItem', position: 1, name: 'LocalClaw', item: `${BASE}/` }, { '@type': 'ListItem', position: 2, name, item: url }] });
const schema = { '@context': 'https://schema.org', '@graph': [
  { '@type': 'WebPage', '@id': `${BASE}/labs#page`, url: `${BASE}/labs`, name: 'LocalClaw Labs — Free Browser AI Playground', description: LABS_DESCRIPTION, inLanguage: 'en', mainEntity: { '@id': `${BASE}/labs#app` }, image: IMAGE },
  { '@type': 'WebApplication', '@id': `${BASE}/labs#app`, name: 'LocalClaw Labs', url: `${BASE}/labs`, description: LABS_DESCRIPTION, applicationCategory: 'EducationalApplication', operatingSystem: 'Web browser', browserRequirements: 'JavaScript, WebAssembly, Workers and local browser file storage. WebGPU required for Qwen3.5 4B.', isAccessibleForFree: true, offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' }, publisher: { '@type': 'Organization', name: 'LocalClaw', url: `${BASE}/` }, featureList: ['Browser-local AI chat', 'Detective interview game', 'Writing remix studio', 'Option scores compared with generated JSON', `${DECISION_EXAMPLES.length} fictional decision situations`], screenshot: IMAGE },
  { '@type': 'FAQPage', '@id': `${BASE}/labs#faq`, mainEntity: LABS_FAQ.map(item => ({ '@type': 'Question', name: item.question, acceptedAnswer: { '@type': 'Answer', text: item.answer } })) },
  breadcrumbs('Labs', `${BASE}/labs`),
] };

const content = `<section id="about-labs" class="labs-discover" aria-labelledby="labs-about-title">
  <nav class="labs-breadcrumb" aria-label="Breadcrumb"><a href="/">LocalClaw</a><span aria-hidden="true">/</span><span>Labs</span></nav>
  <span class="labs-label">Free browser AI playground</span><h2 id="labs-about-title">Run a small LLM in your browser.</h2>
  <p>LocalClaw Labs is a free playground for small language models running on your own device. Download a model once, then try local AI chat, a detective game, a writing remix or a decision experiment. No account, API key or desktop installation is required. Your prompts are not sent to a hosted AI service.</p>
  <div class="labs-facts"><p><strong>5 model presets</strong><span>Qwen3, MiniCPM5 and Qwen3.5</span></p><p><strong>4 experiments</strong><span>Play, write, chat and compare decisions</span></p><p><strong>${DECISION_EXAMPLES.length} situations</strong><span>Shuffle fictional examples, then test them</span></p></div>
  <h3>Start in three steps</h3><ol class="labs-steps"><li><strong>Choose a model.</strong> Start with Qwen3 0.6B Q4 for the smallest download. Check the memory guidance before choosing a larger preset.</li><li><strong>Click Load model.</strong> The weights download from Hugging Face and are cached by your browser when storage is available.</li><li><strong>Try an experiment.</strong> Ask a suspect a question, remix your text, chat, or shuffle a decision situation and compare methods.</li></ol>
  <h3 id="browser-models">Models you can actually run here</h3>${table}
  <p class="labs-help">Download sizes are rounded decimal units. Free-memory figures are estimates, not guarantees. A browser session, context and graphics buffers need memory beyond the weights. These presets are not the complete <a href="/llm-list">LocalClaw model catalogue</a>.</p>
  <h3>Explore four experiments</h3><div class="labs-experiment-links"><a href="/labs#mystery"><strong>The missing masterpiece</strong><span>Interview three fictional suspects. The evidence and solution are authored; the model improvises dialogue.</span></a><a href="/labs#remix"><strong>Remix studio</strong><span>Rewrite your own text in a clear, pirate, movie-trailer or haiku style.</span></a><a href="/labs#chat"><strong>Free local AI chat</strong><span>Explore prompts with a small model. It has no web access or external tools here.</span></a><a href="/labs#decisions"><strong>Decisions / RLCD</strong><span>Compare conditional option scores with written JSON estimates. Neither is calibrated confidence.</span></a></div>
  <p>New to browser inference? <a href="${GUIDE}">Read the practical guide to running an LLM in your browser</a>. For desktop runtimes and bigger models, explore the <a href="/software">local AI software directory</a>.</p>
</section>
<section id="faq" class="labs-discover labs-faq" aria-labelledby="labs-faq-title"><span class="labs-label">Before you start</span><h2 id="labs-faq-title">Browser AI playground FAQ</h2>${LABS_FAQ.map(item => `<details><summary>${escape(item.question)}</summary><p>${escape(item.answer)}</p></details>`).join('\n')}<p class="labs-help">Sources: <a href="https://github.com/ngxson/wllama" target="_blank" rel="noopener noreferrer">wllama</a>, the linked model repositories, <a href="https://openjev.com/" target="_blank" rel="noopener noreferrer">SemIf / OpenJEV</a> and <a href="https://typesafe.ai/blog/introducing-system-one-models-and-jev" target="_blank" rel="noopener noreferrer">TypeSafe’s RLCD introduction</a>. See the <a href="/privacy">privacy policy</a>. Last substantive update: September 21, 2026.</p></section>`;

function write(file, expected) {
  const target = path.join(ROOT, file);
  if (CHECK) { if (!fs.existsSync(target) || fs.readFileSync(target, 'utf8') !== expected) throw new Error(`${file} is stale; run npm run labs:generate.`); }
  else fs.writeFileSync(target, expected);
}
function replace(html, marker, body) {
  const start = `<!-- ${marker}_START -->`, end = `<!-- ${marker}_END -->`;
  if (!html.includes(start) || !html.includes(end)) throw new Error(`Missing ${marker} markers.`);
  return html.slice(0, html.indexOf(start)) + `${start}\n${body}\n${end}` + html.slice(html.indexOf(end) + end.length);
}
let html = fs.readFileSync(path.join(ROOT, 'labs.html'), 'utf8');
html = replace(html, 'LABS_SCHEMA', `<script type="application/ld+json">${json(schema)}</script>`);
html = replace(html, 'LABS_SEARCH_CONTENT', content);
html = html.replace(/<meta name="description" content="[^"]*">/, `<meta name="description" content="${escape(LABS_DESCRIPTION)}">`);
write('labs.html', html);

const guideTitle = 'Run an LLM in Your Browser: Local AI Without an API Key';
const guideDescription = 'A practical guide to browser-local AI: model downloads, WebGPU, privacy, hardware limits and the difference between RLCD and ordinary option scoring.';
const guideSchema = { '@context': 'https://schema.org', '@graph': [{ '@type': 'Article', headline: guideTitle, description: guideDescription, mainEntityOfPage: `${BASE}${GUIDE}`, image: IMAGE, datePublished: '2026-09-21', dateModified: '2026-09-21', inLanguage: 'en', author: { '@type': 'Organization', name: 'LocalClaw', url: `${BASE}/` }, publisher: { '@type': 'Organization', name: 'LocalClaw', url: `${BASE}/` } }, breadcrumbs('Run an LLM in your browser', `${BASE}${GUIDE}`)] };
write('guides/run-llm-in-browser.html', `<!DOCTYPE html>
<html lang="en" class="light"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${guideTitle} | LocalClaw</title><meta name="description" content="${guideDescription}"><meta name="robots" content="index, follow, max-image-preview:large"><link rel="canonical" href="${BASE}${GUIDE}"><meta property="og:type" content="article"><meta property="og:title" content="${guideTitle}"><meta property="og:description" content="${guideDescription}"><meta property="og:url" content="${BASE}${GUIDE}"><meta property="og:image" content="${IMAGE}"><meta name="twitter:card" content="summary_large_image"><meta name="twitter:title" content="${guideTitle}"><meta name="twitter:description" content="${guideDescription}"><meta name="twitter:image" content="${IMAGE}"><meta name="color-scheme" content="light dark"><link rel="icon" href="/images/favicon.png?v=20260211g"><link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Inter:wght@400;500;600;700&display=swap"><link rel="stylesheet" href="/css/labs-20260921.css?v=growth1">${siteNavAssets()}<script type="application/ld+json">${json(guideSchema)}</script></head>
<body class="lc-labs-page"><a class="labs-skip" href="#guide-main">Skip to guide</a>${siteNavigation()}
<main id="guide-main" class="labs-shell labs-guide"><nav class="labs-breadcrumb" aria-label="Breadcrumb"><a href="/">LocalClaw</a><span aria-hidden="true">/</span><span>Run an LLM in your browser</span></nav><header class="labs-intro"><span class="labs-label">Practical guide · September 21, 2026</span><h1>Run an LLM.<br><span>Right in your browser.</span></h1><p>Small models, local computation, no API key.</p></header>
<article class="labs-discover"><h2>The short answer</h2><p>Yes, a website can run a language model on your own computer. The page downloads model weights and a local inference engine, then your browser does the computation. <a href="/labs">LocalClaw Labs</a> puts that into a free playground: chat, writing, a detective game and a decision comparison, with five model presets.</p><p><a class="labs-button labs-button--primary" href="/labs">Open the browser AI playground →</a></p>
<h2>What happens when you click Load model?</h2><ol class="labs-steps"><li>The page loads the <a href="https://github.com/ngxson/wllama" target="_blank" rel="noopener noreferrer">wllama</a> engine, which brings llama.cpp inference into the browser.</li><li>Your chosen GGUF model downloads from its pinned Hugging Face revision. This is a real download, from 397 MB to 3.01 GB in Labs, not a remote chatbot disguised as a local one.</li><li>The browser stores the weights when local storage is available and prepares them for inference. Reloading a cached model still takes initialization time.</li><li>You submit a prompt. WebAssembly runs the engine, with WebGPU acceleration when available. Smaller presets can use the CPU; Labs requires WebGPU for the 4B preset.</li></ol><p>No model downloads automatically on page load. A slow connection can make the first setup take minutes, while browser storage limits or memory pressure can prevent a model from loading.</p>
<h2>Choose the download that fits your device</h2>${table}<p>Start with Qwen3 0.6B Q4 when testing compatibility. Move up only after it runs comfortably. The Q8 version uses more storage than the Q4 version of the same 0.6B model; the number of parameters alone does not determine download size. Qwen3.5 4B is a computer-oriented preset, not a sensible default for a phone.</p><p>Memory guidance is approximate. Available RAM, graphics buffers, context length, browser version and competing tabs all matter. A successful run on one machine does not prove that a model works on every browser or device.</p>
<h2>Try something concrete before judging the model</h2><p>In the detective game, ask each suspect about their alibi and compare the dialogue with the authored case notes. In Remix studio, rewrite the same text in several styles. In free chat, try a short question in your own language and watch the locally generated response.</p><p>For <a href="/labs#decisions">Decisions / RLCD</a>, use Shuffle situation to explore ${DECISION_EXAMPLES.length} fictional scenarios. Support tickets, email checks and Detective Crab puzzles include both clear cases and deliberately incomplete evidence. The button changes the situation, question and choices together; it does not manufacture model results. You can edit any example before running it.</p>
<h2>RLCD is a training objective, not a probability display</h2><p><a href="https://typesafe.ai/blog/introducing-system-one-models-and-jev" target="_blank" rel="noopener noreferrer">TypeSafe describes RLCD</a> as Reinforcement Learning for Calibrated Decisions. Its Jev system should not be confused with a standard language model asked to pick a letter. <a href="https://openjev.com/" target="_blank" rel="noopener noreferrer">SemIf, formerly OpenJEV</a>, is an independent browser experiment; Labs includes its three open-model presets, not Jev weights.</p><p>Labs compares two outputs from the same model. One constrained output token provides relative scores over the allowed options. A second call asks the model to write probability estimates as JSON. The numbers can disagree, and the JSON can be malformed. Neither method establishes calibration: a displayed 80% is not evidence that the answer is correct 80% of the time.</p><p>Timings are measured on your current device. Warm-up is excluded; option scoring runs before JSON; measured prompt caching is disabled. The output instructions and token counts differ. Treat this as an experiment, not a controlled model ranking, a Jev benchmark or a tool for consequential automated decisions.</p>
<h2>What stays local, and what still uses the network?</h2><p>Prompts and answers in Labs stay in the tab and are not sent to an AI service. The page does not use session recordings. Hosting providers still receive ordinary requests for the page, runtime and weights. Reloading clears the conversation, but model files can remain cached. The <a href="/privacy">privacy policy</a> explains the wider website context.</p><p>Unload model releases active model memory. Remove downloaded models, inside How local inference works, removes the listed cached weights. A browser may also evict them to reclaim space. Inference after loading does not require an AI API, but the app does not guarantee offline page reloads. Do not equate local computation with a complete offline-installation guarantee.</p>
<h2>Browser playground or desktop runtime?</h2><p>A browser playground is useful for a quick demo, a lesson, a prompt experiment or a small interactive game. If you need larger models, long conversations, file workflows, persistent tools or runtime tuning, explore the <a href="/software">local AI software directory</a> and the <a href="/llm-list">full LLM catalogue</a>. The free Labs page does not require the paid LocalClaw Mac application.</p><p><a class="labs-button labs-button--primary" href="/labs#decisions">Shuffle a situation and try it →</a></p></article>
</main><footer class="labs-page-footer"><span>© 2026 LocalClaw · Practical guides</span><span><a href="/labs">Labs</a> · <a href="/privacy">Privacy</a></span></footer></body></html>\n`);
console.log(`${CHECK ? 'Verified' : 'Generated'} Labs content: ${MODELS.length} model presets, ${DECISION_EXAMPLES.length} situations, ${LABS_FAQ.length} matching FAQ answers and browser guide.`);
